// tslint:disable-next-line:no-empty-interface
export interface IRootStore {
	// store: IStore
}

// tslint:disable-next-line:arrow-return-shorthand
export const createRootStore = (): IRootStore => {
	return {
		// assign required stores
	};
};
